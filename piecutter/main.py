"""
TODO
cookiecutter/main.py: will cookiecutter.repository.determine_repo_dir work here?
context_file = os.path.join(repo_dir, 'piecutter.py')
then `from context_file import piecutter`; call piecutter() which returns a list of dicts
use those dicts to display prompts, pass resulting keys/values as context to cookiecutter
"""


def piecutter(template):
    print(u"hello world")
